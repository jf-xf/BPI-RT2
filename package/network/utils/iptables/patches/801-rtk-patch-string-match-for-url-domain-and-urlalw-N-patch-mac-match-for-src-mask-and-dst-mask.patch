Index: iptables-1.8.7/extensions/libxt_mac.c
===================================================================
--- iptables-1.8.7.orig/extensions/libxt_mac.c
+++ iptables-1.8.7/extensions/libxt_mac.c
@@ -6,24 +6,44 @@
 #endif
 #include <xtables.h>
 #include <linux/netfilter/xt_mac.h>
+#include <iptables/internal.h>
 
 enum {
-	O_MAC = 0,
+	O_SRC_MAC = 0,
+	O_SRC_MASK = 1,
+	O_DST_MAC = 2,
+	O_DST_MASK = 3,
 };
 
 static void mac_help(void)
 {
 	printf(
-"mac match options:\n"
+"MAC options:\n"
 "[!] --mac-source XX:XX:XX:XX:XX:XX\n"
-"				Match source MAC address\n");
+		"							   Match source MAC address\n"
+		" --src-mask XX:XX:XX:XX:XX:XX\n"
+		"							   Source MAC mask\n"
+		" [!] --mac-dst XX:XX:XX:XX:XX:XX\n"
+		"							   Match destination MAC address\n"
+		" --dst-mask XX:XX:XX:XX:XX:XX\n"
+		"							   Destination MAC mask\n"
+		"\n");
 }
 
 #define s struct xt_mac_info
 static const struct xt_option_entry mac_opts[] = {
-	{.name = "mac-source", .id = O_MAC, .type = XTTYPE_ETHERMAC,
-	 .flags = XTOPT_MAND | XTOPT_INVERT | XTOPT_PUT,
+	{.name = "mac-source", .id = O_SRC_MAC, .type = XTTYPE_ETHERMAC,
+	 .flags = XTOPT_INVERT | XTOPT_PUT,
 	 XTOPT_POINTER(s, srcaddr)},
+	{.name = "src-mask", .id = O_SRC_MASK, .type = XTTYPE_ETHERMAC,
+	  .flags = XTOPT_PUT,
+	  XTOPT_POINTER(s, srcmask)},
+	{.name = "mac-dst", .id = O_DST_MAC, .type = XTTYPE_ETHERMAC,
+	  .flags = XTOPT_INVERT | XTOPT_PUT,
+	  XTOPT_POINTER(s, dstaddr)},
+	{.name = "dst-mask", .id = O_DST_MASK, .type = XTTYPE_ETHERMAC,
+	  .flags = XTOPT_PUT,
+	  XTOPT_POINTER(s, dstmask)},
 	XTOPT_TABLEEND,
 };
 #undef s
@@ -32,9 +52,38 @@ static void mac_parse(struct xt_option_c
 {
 	struct xt_mac_info *macinfo = cb->data;
 
-	xtables_option_parse(cb);
-	if (cb->invert)
-		macinfo->invert = 1;
+	if(cb->entry->id == 0){ //--mac-source
+		xtables_option_parse(cb);
+		if (cb->invert){
+			macinfo->flags |= MAC_SRC_INV;
+		}
+		macinfo->flags |= MAC_SRC;
+	}
+	else if(cb->entry->id == 1){ //--src-mask
+		xtables_option_parse(cb);
+		if (cb->invert){		//note this option cannot use invert
+			xtables_error(PARAMETER_PROBLEM, "--src-mask: cannot use invert!");
+		}
+		macinfo->flags |= SRC_MASK;
+	}
+	else if(cb->entry->id == 2){ //--mac-dst
+		xtables_option_parse(cb);
+		if (cb->invert){
+			macinfo->flags |= MAC_DST_INV;
+		}
+		macinfo->flags |= MAC_DST;
+	}
+	else if(cb->entry->id == 3){ //--dst-mask
+		xtables_option_parse(cb);
+		if (cb->invert){		//note this option cannot use invert
+			xtables_error(PARAMETER_PROBLEM, "--dst-mask: cannot use invert!");
+		}
+		macinfo->flags |= DST_MASK;
+	 }
+	else{
+ 		xtables_error(PARAMETER_PROBLEM, "mac match: unknown option!");
+	}
+
 }
 
 static void
@@ -42,44 +91,69 @@ mac_print(const void *ip, const struct x
 {
 	const struct xt_mac_info *info = (void *)match->data;
 
-	printf(" MAC");
-
-	if (info->invert)
-		printf(" !");
-
-	xtables_print_mac(info->srcaddr);
+	if(info->flags & MAC_SRC){
+		printf(" SRC MAC");
+		if(info->flags & MAC_SRC_INV){
+			printf(" !");
+		}
+		xtables_print_mac(info->srcaddr);
+		if(info->flags & SRC_MASK){
+			printf(" SRC MASK");
+			xtables_print_mac(info->srcmask);
+		}
+	}
+	if(info->flags & MAC_DST){
+		printf(" DST MAC");
+		if(info->flags & MAC_DST_INV){
+			printf(" !");
+		}
+		xtables_print_mac(info->dstaddr);
+
+		if(info->flags & DST_MASK){
+			printf(" DST MASK");
+			xtables_print_mac(info->dstmask);
+		}
+	}
 }
 
 static void mac_save(const void *ip, const struct xt_entry_match *match)
 {
 	const struct xt_mac_info *info = (void *)match->data;
 
-	if (info->invert)
-		printf(" !");
-
-	printf(" --mac-source ");
-	xtables_print_mac(info->srcaddr);
-}
-
-static void print_mac_xlate(const unsigned char *macaddress,
-			    struct xt_xlate *xl)
-{
-	unsigned int i;
-
-	xt_xlate_add(xl, "%02x", macaddress[0]);
-	for (i = 1; i < ETH_ALEN; ++i)
-		xt_xlate_add(xl, ":%02x", macaddress[i]);
-}
-
-static int mac_xlate(struct xt_xlate *xl,
-		     const struct xt_xlate_mt_params *params)
-{
-	const struct xt_mac_info *info = (void *)params->match->data;
-
-	xt_xlate_add(xl, "ether saddr%s ", info->invert ? " !=" : "");
-	print_mac_xlate(info->srcaddr, xl);
-
-	return 1;
+	if(info->flags & MAC_SRC){
+		if(info->flags & MAC_SRC_INV){
+			printf(" !");
+		}
+		printf(" --mac-source");
+		xtables_print_mac(info->srcaddr);
+
+		if(info->flags & SRC_MASK){
+			printf(" --src-mask");
+			xtables_print_mac(info->srcmask);
+		}
+	}
+	if(info->flags & MAC_DST){
+		if(info->flags & MAC_DST_INV){
+			printf(" !");
+		}
+		printf(" --mac-dst");
+		xtables_print_mac(info->dstaddr);
+
+		if(info->flags & DST_MASK){
+			printf(" --dst-mask");
+			xtables_print_mac(info->dstmask);
+		}
+	}
+}
+
+/* Final check; must have specified --mac. */
+/* Final check; must have specified --mac-source or --mac-dst. */
+static void final_check(unsigned int flags)
+{
+	int check;
+	check = flags & (MAC_SRC | MAC_DST);
+	if (!check)
+		xtables_error(PARAMETER_PROBLEM, "You must specify `--mac-source' or 'mac-dst'");
 }
 
 static struct xtables_match mac_match = {
@@ -90,10 +164,10 @@ static struct xtables_match mac_match =
 	.userspacesize	= XT_ALIGN(sizeof(struct xt_mac_info)),
 	.help		= mac_help,
 	.x6_parse	= mac_parse,
+	.final_check    = final_check,
 	.print		= mac_print,
 	.save		= mac_save,
 	.x6_options	= mac_opts,
-	.xlate		= mac_xlate,
 };
 
 void _init(void)
Index: iptables-1.8.7/extensions/libxt_string.c
===================================================================
--- iptables-1.8.7.orig/extensions/libxt_string.c
+++ iptables-1.8.7/extensions/libxt_string.c
@@ -35,21 +35,31 @@ enum {
 	O_ICASE,
 	O_STRING,
 	O_HEX_STRING,
-	F_STRING     = 1 << O_STRING,
+	O_URL,
+	O_DOMAIN,
+	O_URLALW,
+	F_STRING = 1 << O_STRING,
 	F_HEX_STRING = 1 << O_HEX_STRING,
-	F_OP_ANY     = F_STRING | F_HEX_STRING,
+	F_URL = 1 << O_URL,
+	F_DOMAIN = 1 << O_DOMAIN,
+	F_URLALW = 1 << O_URLALW,
+	F_OP_ANY = F_STRING | F_HEX_STRING | F_URL | F_DOMAIN | F_URLALW
 };
 
 static void string_help(void)
 {
 	printf(
-"string match options:\n"
-"--from                       Offset to start searching from\n"
-"--to                         Offset to stop searching\n"
-"--algo                       Algorithm\n"
-"--icase                      Ignore case (default: 0)\n"
-"[!] --string string          Match a string in a packet\n"
-"[!] --hex-string string      Match a hex string in a packet\n");
+		"string match options:\n"
+		"--from 					  Offset to start searching from\n"
+		"--to						  Offset to stop searching\n"
+		"--algo 					  Algorithm\n"
+		"--icase					  Ignore case (default: 0)\n"
+		"[!] --string string		  Match a string in a packet\n"
+		"[!] --hex-string string	  Match a hex string in a packet\n"
+		"[!] --url string			  Match a url string in a packet\n" // Added by Mason Yu for URL Blocking
+		"[!] --domain string		  Match a Domain string in a packet\n" // Added by Mason Yu for Domain Blocking
+		"[!] --urlalw string		   Match a url allow string in a packet\n" //Added by alex for URL allowing
+	);
 }
 
 #define s struct xt_string_info
@@ -65,6 +75,12 @@ static const struct xt_option_entry stri
 	{.name = "hex-string", .id = O_HEX_STRING, .type = XTTYPE_STRING,
 	 .flags = XTOPT_INVERT, .excl = F_STRING},
 	{.name = "icase", .id = O_ICASE, .type = XTTYPE_NONE},
+	{.name = "url", .id = O_URL, .type = XTTYPE_STRING,
+	 .flags = XTOPT_INVERT, .excl = F_HEX_STRING},
+	{.name = "domain", .id = O_DOMAIN, .type = XTTYPE_STRING,
+	 .flags = XTOPT_INVERT, .excl = F_HEX_STRING},
+	{.name = "urlalw", .id = O_URLALW, .type = XTTYPE_STRING,
+	 .flags = XTOPT_INVERT, .excl = F_HEX_STRING},
 	XTOPT_TABLEEND,
 };
 #undef s
@@ -79,9 +95,19 @@ static void string_init(struct xt_entry_
 static void
 parse_string(const char *s, struct xt_string_info *info)
 {	
+	int i = 0;
 	/* xt_string does not need \0 at the end of the pattern */
 	if (strlen(s) <= XT_STRING_MAX_PATTERN_SIZE) {
 		strncpy(info->pattern, s, XT_STRING_MAX_PATTERN_SIZE);
+
+		//QL 20110629: remove "/r/n" from pattern
+		while(1) {
+				if ((info->pattern[i]=='\r') || (info->pattern[i]=='\n') || info->pattern[i]=='\0')
+						break;
+				i++;
+		}
+		info->pattern[i] = '\0';
+
 		info->patlen = strnlen(s, XT_STRING_MAX_PATTERN_SIZE);
 		return;
 	}
@@ -182,6 +208,8 @@ static void string_parse(struct xt_optio
 			else
 				stringinfo->u.v1.flags |= XT_STRING_FLAG_INVERT;
 		}
+
+		stringinfo->str_flag = XT_STRING_FLAG_STRING;
 		break;
 	case O_HEX_STRING:
 		parse_hex_string(cb->arg, stringinfo);  /* sets length */
@@ -191,6 +219,8 @@ static void string_parse(struct xt_optio
 			else
 				stringinfo->u.v1.flags |= XT_STRING_FLAG_INVERT;
 		}
+
+		stringinfo->str_flag = XT_STRING_FLAG_HEX_STRING;
 		break;
 	case O_ICASE:
 		if (revision == 0)
@@ -199,6 +229,39 @@ static void string_parse(struct xt_optio
 
 		stringinfo->u.v1.flags |= XT_STRING_FLAG_IGNORECASE;
 		break;
+	case O_URL:
+		parse_string(cb->arg, stringinfo);
+		if (cb->invert) {
+				if (revision == 0)
+						stringinfo->u.v0.invert = 1;
+				else
+						stringinfo->u.v1.flags |= XT_STRING_FLAG_INVERT;
+		}
+
+		stringinfo->str_flag = XT_STRING_FLAG_URL;
+		break;
+	case O_DOMAIN:
+		parse_string(cb->arg, stringinfo);
+		if (cb->invert) {
+				if (revision == 0)
+						stringinfo->u.v0.invert = 1;
+				else
+						stringinfo->u.v1.flags |= XT_STRING_FLAG_INVERT;
+		}
+
+		stringinfo->str_flag = XT_STRING_FLAG_DOMAIN;
+		break;
+	case O_URLALW:
+		parse_string(cb->arg, stringinfo);
+		if (cb->invert) {
+				if (revision == 0)
+						stringinfo->u.v0.invert = 1;
+				else
+						stringinfo->u.v1.flags |= XT_STRING_FLAG_INVERT;
+		}
+
+		stringinfo->str_flag = XT_STRING_FLAG_URLALW;
+		break;
 	}
 }
 
@@ -207,7 +270,7 @@ static void string_check(struct xt_fchec
 	if (!(cb->xflags & F_OP_ANY))
 		xtables_error(PARAMETER_PROBLEM,
 			   "STRING match: You must specify `--string' or "
-			   "`--hex-string'");
+			   "`--hex-string' or `--url' or `--domain' or `--urlalw'");
 }
 
 /* Test to see if the string contains non-printable chars or quotes */
Index: iptables-1.8.7/include/linux/netfilter/xt_mac.h
===================================================================
--- iptables-1.8.7.orig/include/linux/netfilter/xt_mac.h
+++ iptables-1.8.7/include/linux/netfilter/xt_mac.h
@@ -1,8 +1,44 @@
 #ifndef _XT_MAC_H
 #define _XT_MAC_H
 
+/*
+oringinal version
+*/
+/*
+
 struct xt_mac_info {
     unsigned char srcaddr[ETH_ALEN];
     int invert;
 };
+*/
+
+////////////////////////////////////////
+
+/*
+hack version
+*/
+/*linux-2.6.19*/
+#define MAC_SRC                 0x01    /* Match source MAC address */
+#define SRC_MASK                0x02    /* Source MAC mask */
+#define MAC_DST                 0x04    /* Match destination MAC address */
+#define DST_MASK                0x08    /* Destination MAC mask */
+#define MAC_SRC_INV             0x10    /* Negate the condition */
+#define SRC_MASK_INV            0x20    /* Negate the condition */
+#define MAC_DST_INV             0x40    /* Negate the condition */
+#define DST_MASK_INV            0x80    /* Negate the condition */
+
+struct xt_mac_info {
+    unsigned char srcaddr[ETH_ALEN];
+    /*linux-2.6.19*/
+    // Kaohj -- add mac mask
+    unsigned char srcmask[ETH_ALEN];
+    // Added by Mason Yu for dst MAC
+    unsigned char dstaddr[ETH_ALEN];
+    // Kaohj -- add mac mask
+    unsigned char dstmask[ETH_ALEN];
+    //int invert;
+    /* Flags from above */
+    u_int8_t flags;
+};
+
 #endif /*_XT_MAC_H*/
Index: iptables-1.8.7/include/linux/netfilter/xt_string.h
===================================================================
--- iptables-1.8.7.orig/include/linux/netfilter/xt_string.h
+++ iptables-1.8.7/include/linux/netfilter/xt_string.h
@@ -11,12 +11,23 @@ enum {
 	XT_STRING_FLAG_IGNORECASE	= 0x02
 };
 
+// Magician: Extended to URL, Domain, and URLALW
+enum {
+	XT_STRING_FLAG_STRING = 1,
+	XT_STRING_FLAG_HEX_STRING,
+	XT_STRING_FLAG_URL,
+	XT_STRING_FLAG_DOMAIN,
+	XT_STRING_FLAG_URLALW
+};
+
 struct xt_string_info {
 	__u16 from_offset;
 	__u16 to_offset;
-	char	  algo[XT_STRING_MAX_ALGO_NAME_SIZE];
-	char 	  pattern[XT_STRING_MAX_PATTERN_SIZE];
-	__u8  patlen;
+	char algo[XT_STRING_MAX_ALGO_NAME_SIZE];
+	char pattern[XT_STRING_MAX_PATTERN_SIZE];
+	__u8 patlen;
+	__u8 str_flag; // Magician: Extended to URL, Domain, and URLALW
+
 	union {
 		struct {
 			__u8  invert;
